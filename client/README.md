# Клиентская библиотека any_to_md

Python клиент для работы с сервисом конвертации документов в Markdown. Поддерживает асинхронную пакетную обработку, мониторинг статуса и автоматическое скачивание результатов.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install -r client/requirements.txt
```

### Отправка файлов на конвертацию

```bash
python client/async_submit.py test_files/*.pdf
```

### Мониторинг и скачивание результатов

```bash
python client/async_monitor.py
```

## 📦 Структура клиента

### Основные компоненты

- **`base_client.py`** - Базовый класс клиента с основной функциональностью
- **`async_submit.py`** - Асинхронная параллельная загрузка файлов с прогресс-баром и retry логикой
- **`async_monitor.py`** - Асинхронный мониторинг и скачивание с параллельной обработкой
- **`cleanup_history.py`** - Универсальная очистка истории задач и БД сервера
- **`cleanup_tasks.py`** - Детализированная очистка с расширенными фильтрами и статистикой

## 📥 Установка

### Требования

- Python 3.7+
- pip (менеджер пакетов Python)

### Установка зависимостей

```bash
# Клонировать репозиторий
git clone https://git.inresys.net/ibo/any-to-md-service.git
cd any_to_md

# Установить зависимости клиента
pip install -r client/requirements.txt
```

### Зависимости

- **requests** >= 2.31.0 - Синхронные HTTP запросы
- **aiohttp** >= 3.8.0 - Асинхронные HTTP запросы  
- **aiofiles** >= 23.0.0 - Асинхронная работа с файлами

## 🎯 Примеры использования

### 1. Асинхронная отправка файлов

```bash
# Отправить все PDF файлы из папки
python client/async_submit.py test_files/*.pdf

# Отправить файлы разных форматов
python client/async_submit.py test_files/*.pdf test_files/*.docx test_files/*.xlsx

# Указать конкретные файлы
python client/async_submit.py document1.pdf presentation.pptx table.xlsx

# Использовать кастомный сервер и файл задач
python client/async_submit.py --server http://localhost:8080 --tasks-file my_tasks.json files/*.pdf
```

**Особенности:**
- Параллельная загрузка до 3 файлов одновременно
- Прогресс-бар с отображением скорости и времени
- Автоматические retry (3 попытки) при ошибках
- Проверка дубликатов по MD5 хешу
- Сохранение состояния в JSON файл

### 2. Асинхронный мониторинг и скачивание

```bash
# Мониторинг с настройками по умолчанию
python client/async_monitor.py

# Указать интервал проверки и максимум итераций
python client/async_monitor.py --interval 5 --max-iterations 100

# Использовать кастомный сервер и папку для результатов
python client/async_monitor.py --server http://localhost:8080 --output-dir ./results

# Использовать другой файл задач
python client/async_monitor.py --tasks-file my_tasks.json
```

**Особенности:**
- Параллельное скачивание до 5 файлов одновременно
- Автоматические retry при сетевых ошибках
- Цветной вывод статусов (✅ успех, ❌ ошибка, ⏳ обработка)
- Детальная статистика по каждой итерации
- Автоматическое определение формата (ZIP или текст с S3 URL)

### 3. Очистка истории задач

#### cleanup_history.py - Универсальная очистка

```bash
# Очистить только скачанные задачи (по умолчанию)
python client/cleanup_history.py

# Удалить все задачи
python client/cleanup_history.py --all

# Проверить статусы на сервере и пометить failed
python client/cleanup_history.py --check-server

# Очистить failed задачи из БД сервера
python client/cleanup_history.py --clean-failed

# Полная очистка: проверка на сервере + очистка БД
python client/cleanup_history.py --check-server --clean-failed

# Использовать другой файл задач
python client/cleanup_history.py my_tasks.json --all
```

#### cleanup_tasks.py - Детализированная очистка

```bash
# Удалить фантомные задачи (старые неактивные)
python client/cleanup_tasks.py --status_downloaded false

# Показать только статистику без удаления
python client/cleanup_tasks.py --status_downloaded all --statistics

# Удалить задачи старше 24 часов
python client/cleanup_tasks.py --status_downloaded sent --older-than 24

# Удалить ошибочные задачи
python client/cleanup_tasks.py --status_downloaded "failed,not_found"

# Проверить существование на сервере перед удалением
python client/cleanup_tasks.py --status_downloaded false --check-server
```

## 📝 Использование из Python кода

### Базовый клиент

```python
from client.base_client import BaseClient

# Инициализация клиента
client = BaseClient(base_url="http://localhost:8080")

# Конвертация одного файла
task_id = client.convert_file("document.pdf")
print(f"Задача создана: {task_id}")

# Проверка статуса
status = client.check_status(task_id)
print(f"Статус: {status['status']}")

# Скачивание результата
if status['status'] == 'completed':
    client.download_result(task_id, "output.zip")
```

### Асинхронная обработка

```python
import asyncio
from client.async_submit import submit_files_async
from client.async_monitor import monitor_tasks_async

async def process_documents():
    # Отправка файлов
    files = ["doc1.pdf", "doc2.docx", "doc3.xlsx"]
    await submit_files_async(files, server_url="http://localhost:8080")
    
    # Мониторинг и скачивание
    await monitor_tasks_async(
        tasks_file="pending_tasks.json",
        output_dir="./results",
        max_iterations=50
    )

# Запуск
asyncio.run(process_documents())
```

## 📋 Формат файла задач

Клиент сохраняет информацию о задачах в JSON файле:

```json
{
  "tasks": [
    {
      "task_id": "abc123-def456-789",
      "file_path": "test_files/document.pdf",
      "file_name": "document.pdf",
      "format": "pdf",
      "submitted_at": "2025-01-14T10:30:45.123456",
      "downloaded": false,
      "md5_hash": "a3f2d1b4e5c2..."
    }
  ],
  "last_updated": "2025-01-14T10:30:45.123456",
  "server": "http://localhost:8080",
  "last_checked": "2025-01-14T10:31:00.000000",
  "last_cleaned": "2025-01-14T09:00:00.000000"
}
```

### Система статусов

| Статус | Описание | Когда устанавливается |
|--------|----------|----------------------|
| `false` | Фантомная задача | Старые задачи от предыдущих сессий |
| `"sent"` | Отправлено на сервер | При загрузке через async_submit.py |
| `"processing"` | В обработке | При проверке статуса на сервере |
| `"failed"` | Ошибка обработки | Сервер вернул ошибку |
| `"not_found"` | Не найдена | HTTP 404 от сервера |
| `true` | Успешно скачано | После скачивания результата |

## ⚙️ Конфигурация

### Параметры командной строки

#### async_submit.py
- `--server URL` - URL сервера (по умолчанию: http://localhost:8080)
- `--tasks-file FILE` - Файл для сохранения задач (по умолчанию: client/pending_tasks.json)
- `--max-workers N` - Количество параллельных загрузок (по умолчанию: 3)

#### async_monitor.py
- `--server URL` - URL сервера (по умолчанию: http://localhost:8080)
- `--tasks-file FILE` - Файл с задачами (по умолчанию: client/pending_tasks.json)
- `--output-dir DIR` - Папка для результатов (по умолчанию: test_results)
- `--interval SEC` - Интервал проверки в секундах (по умолчанию: 10)
- `--max-iterations N` - Максимум итераций (по умолчанию: 60)
- `--max-workers N` - Количество параллельных скачиваний (по умолчанию: 5)

#### cleanup_history.py
- `file` - Путь к файлу задач (по умолчанию: demo_pending_tasks.json)
- `--all` - Удалить все задачи
- `--check-server` - Проверить статусы на сервере
- `--clean-failed` - Очистить failed из БД сервера
- `--server-url URL` - URL сервера (по умолчанию: http://localhost:8080)

#### cleanup_tasks.py
- `--status_downloaded STATUS` - Статус для фильтрации (обязательный)
- `--file FILE` - Файл задач (по умолчанию: client/pending_tasks.json)
- `--older-than HOURS` - Фильтр по возрасту
- `--check-server` - Проверить на сервере
- `--statistics` - Только показать статистику
- `--server-url URL` - URL сервера (по умолчанию: http://localhost:8080)

## 🔍 Поддерживаемые форматы

| Формат | Расширение | Примечание |
|--------|------------|------------|
| PDF | .pdf | Документы с изображениями |
| Word | .doc, .docx | С таблицами и изображениями |
| OpenDocument | .odt | Текстовые документы |
| RTF | .rtf | Rich Text Format |
| PowerPoint | .pptx | Презентации |
| Excel | .xls, .xlsx | Таблицы |
| EPUB | .epub | Электронные книги |

## 📥 Формат результатов

### Без S3 конфигурации
- Сервер возвращает **ZIP архив** с результатами
- Клиент сохраняет файл с расширением `.zip`
- Архив содержит `document.md` и папку `images/`

### С включенным S3
- Сервер возвращает **текстовый файл** с S3 URL
- Клиент сохраняет файл с расширением `.txt`
- Файл содержит прямую ссылку для скачивания из S3

## 🆘 Устранение неполадок

### Ошибка подключения
```
ConnectionError: Failed to connect to server
```
**Решение**: Убедитесь что сервис запущен на правильном порту

### Таймаут при конвертации
```
TimeoutError: Conversion took too long
```
**Решение**: Увеличьте таймаут или количество retry попыток

### Файл не найден
```
FileNotFoundError: File not found
```
**Решение**: Проверьте путь к файлу относительно текущей директории

## 📄 Лицензия

MIT License - см. файл LICENSE в корне проекта