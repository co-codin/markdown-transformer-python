# Конвертер документов в Markdown

Сервис на базе FastAPI для конвертации различных форматов документов в Markdown с извлечением изображений
и сохранением их в s3.

## 🚀 Быстрый старт

### Использование Docker (Рекомендуется)

```bash
# Клонируем репозиторий
git clone ssh://git@git.inresys.net:2222/ibo/any-to-md-service.git
cd any-to-md-service

# Копируем пример окружения
cp .env.example .env

# Запускаем сервис
docker-compose up -d

# Сервис будет доступен по адресу http://localhost:8000
# Примечание: Если порт 8000 занят, отредактируйте docker-compose.yml и измените 
# маппинг портов с "8000:8000" на "8080:8000" или другой порт
```

### Локальная установка

```bash
# Требования: Python 3.11+

# Клонирование и настройка
git clone ssh://git@git.inresys.net:2222/ibo/any-to-md-service.git
cd any-to-md-service

# Создаем виртуальное окружение
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Устанавливаем зависимости
pip install -r requirements.txt

# Опционально: Установка marker-pdf для расширенной поддержки форматов
pip install marker-pdf
# Примечание: При первом запуске marker-pdf автоматически скачает модели (~2GB)

# Копируем пример окружения
cp .env.example .env

# Запускаем сервис
python app/main.py
```

## ✨ Возможности

- **Поддержка множества форматов**: Конвертация 9 форматов документов в чистый Markdown
- **Извлечение изображений**: Автоматическое извлечение и организация встроенных изображений
- **Асинхронная обработка**: Неблокирующая конвертация с отслеживанием статуса в реальном времени
- **Умный вывод**: Возвращает организованный ZIP с `document.md` и папкой `images/`
- **Интеграция с S3**: Опциональное облачное хранилище для конвертированных изображений
- **Автоочистка**: Автоматическое удаление старых результатов конвертации
- **RESTful API**: Простые HTTP эндпоинты с документацией OpenAPI
- **Работа в Docker**: Контейнеризация готовая для продакшена

## 📄 Поддерживаемые форматы

### Форматы документов
- **DOC** - Устаревший формат Microsoft Word
- **DOCX** - Современный формат Microsoft Word  
- **ODT** - OpenDocument текст
- **RTF** - Rich Text Format
- **PDF** - Portable Document Format
- **EPUB** - Электронные книги (⚠️ Внимание: Большие EPUB файлы могут обрабатываться долго - тестовый 18-страничный EPUB обрабатывался 146 секунд)
- **PPTX** - Презентации PowerPoint
- **XLS** - Устаревшие таблицы Excel
- **XLSX** - Современные таблицы Excel

### Примечания по производительности
- Большинство форматов конвертируются за секунды  если их несколько страниц
- **EPUB файлы**: Время обработки зависит от сложности контента. Наши тесты показывают:
  - Маленькие EPUB (< 10 страниц): 60-120 секунд
  - Средние EPUB (10-50 страниц): 3-5 минуты
  - Пример: 18-страничный тестовый файл (443KB) обрабатывался 146 секунд через Marker конвертер

## ☁️ Интеграция с S3 (Опционально)

Когда S3 настроен, все извлеченные изображения автоматически загружаются в облачное хранилище, а файл Markdown содержит прямые S3 URL вместо локальных путей. Это позволяет просматривать файл Markdown из любого места с доступом в интернет.

### Настройка S3

1. **Установите переменные окружения в `.env`:**
```env
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_STORAGE_BUCKET_NAME=your-bucket-name
AWS_S3_REGION_NAME=us-east-1
AWS_S3_ENDPOINT_URL=https://s3.amazonaws.com  # Опционально, для S3-совместимых сервисов
```

2. **Для Docker развертываний:**
   - Обновите `docker-compose.yml` для передачи S3 переменных (см. раздел Устранение неполадок)
   - Или используйте: `docker-compose --env-file .env up -d`

3. **Без S3:**
   - Изображения сохраняются локально в ZIP архиве
   - Markdown использует относительные пути: `./images/image.png`

## 🔧 Системные требования

### Для Docker
- Docker и Docker Compose

### Для локальной установки
- Python 3.11+
- pip install -r requirements.txt
- pip install marker-pdf (опционально, для расширенной поддержки форматов, скачает ~2GB моделей)
- LibreOffice (для конвертации офисных форматов через PDF)

## 📡 Использование API

### 1. Конвертация документа

```bash
curl -X POST "http://localhost:8000/api/v1/convert" \
  -H "accept: application/json" \
  -F "file=@document.pdf"
```

**Ответ:**
```json
{
  "task_id": "123e4567-e89b-12d3-a456-426614174000",
  "status": "pending",
  "message": "Task queued for processing"
}
```

### 2. Проверка статуса конвертации

```bash
curl "http://localhost:8000/api/v1/task/{task_id}"
```

**Ответ:**
```json
{
  "task_id": "123e4567-e89b-12d3-a456-426614174000",
  "status": "completed",
  "message": "Conversion completed successfully"
}
```

### 3. Скачивание результата

```bash
curl -O "http://localhost:8000/api/v1/download/{task_id}"
# Создает: document_converted.zip
```

### Пример использования Python клиента

```python
from client import AnyToMdClient

client = AnyToMdClient("http://localhost:8000")
result = client.convert_and_wait("document.pdf", "output.zip")
print(f"Конвертация завершена: {result}")
```

## ⚙️ Конфигурация

Создайте файл `.env` из `.env.example`:

### Базовые настройки
```env
# Сервер
HOST=0.0.0.0
PORT=8000
DEBUG=False

# Лимиты
MAX_FILE_SIZE_MB=10
CLEANUP_DAYS=7
```

### Интеграция с S3 (Опционально)
```env
# AWS S3 или совместимое хранилище
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_STORAGE_BUCKET_NAME=your-bucket
AWS_S3_REGION_NAME=us-east-1
AWS_S3_ENDPOINT_URL=https://s3.amazonaws.com
S3_FOLDER_PREFIX=markdown-images
```

## 📊 Документация API

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **Проверка здоровья**: http://localhost:8000/health

## 🐳 Docker развертывание

### Продакшен развертывание

```bash
# Сборка оптимизированного образа
docker build -t any-to-md:latest .

# Запуск с docker-compose
docker-compose -f docker-compose.yml up -d

# Проверка логов
docker-compose logs -f

# Остановка сервиса
docker-compose down
```

### Переменные окружения

Сервис может быть настроен через переменные окружения в `docker-compose.yml` или файле `.env`.

## 📁 Формат вывода

Каждая конвертация возвращает ZIP архив если type_results="test", содержащий:

```
converted_document.zip
├── document.md          # Конвертированный Markdown
└── images/             # Извлеченные изображения (если есть)
    ├── image1.png
    ├── image2.jpg
    └── ...
```

Каждая конвертация возвращает ZIP архив если type_results="norm" (по умолчанию выставлен) содержащий:

```
converted_document.zip
 document.md          # Конвертированный Markdown

```


Изображения ссылаются с использованием относительных путей: `![alt text](./images/image.png)`


## 🛠️ Клиентские утилиты

### Демонстрационные скрипты

1. **Пакетная отправка файлов**:
```bash
python client/demo_using_client.py
```
Отправляет тестовые файлы всех поддерживаемых форматов на конвертацию.

2. **Мониторинг и скачивание результатов**:
```bash
python client/monitor_demo.py [интервал_секунд]
```
Мониторит статус задач и автоматически скачивает готовые результаты.

### Утилиты управления

3. **Очистка истории задач**:
```bash
# Удалить только скачанные задачи
python client/cleanup_history.py

# Удалить все задачи
python client/cleanup_history.py --all

# Очистить конкретный файл
python client/cleanup_history.py my_tasks.json
```
Утилита для очистки файла истории задач от старых записей. Полезна при длительном использовании клиента.

## 🔍 Устранение неполадок

### Распространенные проблемы

1. **Ошибка "Неподдерживаемый формат"**
   - Проверьте, что расширение файла в списке поддерживаемых
   - Убедитесь, что файл не поврежден

2. **Таймаут больших файлов**
   - Увеличьте `MAX_FILE_SIZE_MB` в `.env`
   - Для файлов >50MB рассмотрите их разделение

3. **Проблемы с памятью Docker**
   - Увеличьте лимит памяти Docker до 4GB
   - Уменьшите `MAX_FILE_SIZE_MB`

4. **Отсутствующие изображения в выводе**
   - Проверьте, что в оригинальном документе есть встроенные изображения
   - Проверьте достаточность места на диске

5. **Ошибка базы данных SQLite в Docker**
   - База данных создается внутри контейнера
   - Для сохранения данных между перезапусками добавьте том:
     ```yaml
     volumes:
       - ./data:/app/data
     ```
   - Или используйте упрощенный `docker-compose.simple.yml` без сохранения базы данных

6. **Интеграция S3 не работает в Docker**
   - По умолчанию Docker не передает переменные окружения S3
   - Для включения хранилища изображений S3 в Docker добавьте в docker-compose.yml:
     ```yaml
     environment:
       - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
       - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
       - AWS_STORAGE_BUCKET_NAME=${AWS_STORAGE_BUCKET_NAME}
       - AWS_S3_REGION_NAME=${AWS_S3_REGION_NAME}
       - AWS_S3_ENDPOINT_URL=${AWS_S3_ENDPOINT_URL}
     ```
   - Или используйте `docker-compose --env-file .env up -d` для загрузки из файла .env
   - Без конфигурации S3 изображения сохраняются локально в ZIP архиве

7. **Медленная первая конвертация PDF в Docker** (ИСПРАВЛЕНО)
   - ~~Первая конвертация PDF занимает 4-5 минут из-за загрузки OCR модели~~
   - OCR теперь отключен по умолчанию для быстрого запуска
   - OCR нужен только для сканированных PDF без текстового слоя
   - Для включения OCR для сканированных документов установите `pipeline_options.do_ocr = True`

8. **Конфликты портов**
   - Порт по умолчанию 8000 может быть занят другими сервисами
   - Измените маппинг портов в docker-compose.yml: `"8080:8000"`
   - Обновите вызовы API для использования нового порта
